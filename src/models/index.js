const { Board } = require("./board");
const { Role } = require("./role");
const { Token } = require("./token");
const { User } = require("./user");

module.exports = {
	Role,
	User,
	Token,
	Board
};
